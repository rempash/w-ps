
we need to somehow know our users, on mobile we need to generate a unique token when we open the app first time and send it to the backend. Backend should store this token in the database and associate it with the user. 
